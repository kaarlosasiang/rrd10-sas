# Reduce Docker build context for backend image builds
# Keep apps/backend and packages/validators; ignore everything else that's large or irrelevant

# VCS and OS
.git
.gitignore
.DS_Store

# Logs and caches
*.log
npm-debug.log*
pnpm-debug.log*
.cache
**/.cache
coverage

# Environment files (pass via -e at runtime instead)
.env
**/.env
.env.*
**/.env.*

# Node install and build outputs
node_modules
**/node_modules
dist
**/dist

# Monorepo folders not needed for backend image
apps/frontend
apps/admin
packages/ui
scripts

# Temporary directories
.tmp
**/.tmp
